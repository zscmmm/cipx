# cipx — 主入口

`cipx` 是 CIP 提取的统一入口模块，提供 `CIPX` 类封装所有文件类型的提取流程。

::: cipx.cipx.CIPX
    options:
      members:
        - extract
        - from_image
        - from_pdf
        - from_epub
        - from_archive
